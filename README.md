# pre-code

`pre-code` 是一个开发前定向 skill。

触发规则：

- 只在用户明确说 `pre-code` 或明确要求调用这个 skill 时使用
- 不主动自动触发

用途：

- 改功能前先梳理架构和业务流
- 修 bug 前先判断问题层级
- 先收敛边界场景、风险边界、验收标准

特点：

- 架构优先
- 流程图优先
- 边界场景动态生成
- 信息不足时先给推测链路，缺口标 `待确认`
- 输出 HTML 卡片，适合快速扫读

核心文件：

- `SKILL.md`：skill 定义
- `preview.html`：展示预览
